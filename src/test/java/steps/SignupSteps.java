package steps;

import io.cucumber.java.en.Then;
import io.cucumber.java.en.When;
import runners.RunCucumberTest;

public class SignupSteps extends RunCucumberTest {

    //LoginPage loginpage = new LoginPage(driver);

    @When("eu preencho o formulario de cadastro")
    public void eu_preencho_o_formulario_de_cadastro() {
    }

    @When("clico em registrar")
    public void clico_em_registrar() {
    }

    @Then("vejo a mensagem de cadastro realizado com sucesso")
    public void vejo_a_mensagem_de_cadastro_realizado_com_sucesso() {
    }
}
